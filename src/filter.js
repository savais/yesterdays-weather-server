



const paramList = ['tempC', 'windSpeed', 'windDirection', 'cloudCoverage', 'totalCloudCoverage', 'rain_1h']


/**
 * filterData
 * Filters data to only include required measurements
 * 
 * @param {object} today
 * @param {object} yesterday
 * @param {object} daybefore
 * 
 * @returns {object} a filtered object containing today, yesterday and daybefore
 */

const filterData = (today, yesterday, daybefore) => {

}